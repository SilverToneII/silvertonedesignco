/**
 * Barrel export for motion components.
 * PageTransition lands later (v2 per §13.5).
 */
export { LenisProvider, useScrollTo } from './LenisProvider'
export { RevealOnScroll } from './RevealOnScroll'
